# 디자인 노트

## 목표
- 전체 UI는 "다크 네이비 리퀴드 글래스"를 기본 테마로 유지한다.
- 구성 요소별 예외색(회색 기본값, 임시 오버라이드)을 만들지 않는다.
- 같은 역할의 컴포넌트는 같은 토큰을 공유한다.

## 타깃 환경
- iOS Safari standalone 웹앱(PWA)
- iPad 10세대 가로모드 기준 약 1180x820
- 라이트/다크 모두 지원하되, 다크도 네이비 기반으로 유지

## 토큰 시스템 규칙
- 색상은 CSS 변수만 사용한다: `:root`의 토큰을 우선 사용.
- 표면 계층:
  - `--surface-soft`: 카드/패널 기본 유리 표면
  - `--surface-mid`: 헤더/행/보조 컨테이너
  - `--surface-strong`: 활성 탭/선택 상태
- 경계 계층:
  - `--surface-border`: 기본 외곽선
  - `--surface-border-strong`: 활성/강조 외곽선
- 텍스트 계층:
  - `--text-main`: 본문 기본
  - `--text-soft`: 보조 본문
  - `--hint-muted`: 라벨/힌트/빈 상태

## 모달 일관성 규칙
- 아래 모달은 동일한 표면 토큰을 공유한다.
  - 변수 입력 모달
  - 변수 관리 모달
  - 태그 관리 모달
- 모달 내부의 버튼, 탭, 행, 드롭다운, 힌트 텍스트는 다음 공통 규칙을 따른다.
  - 버튼: 네이비 그라디언트 또는 `--surface-strong`
  - 드롭다운/입력: 유리 배경 + `--surface-border`
  - 힌트/보조 텍스트: `--hint-muted`
  - 관리 탭/섹션/행: `--surface-soft`, `--surface-mid`, `--surface-border`

## 금지 사항
- 컴포넌트 단위 임시 오버라이드로 색을 땜질하지 않는다.
- 시스템 회색 고정값(`#f2f2f7`, `#2c2c2e`, `#3a3a3c`, `#8e8e93`)을 신규 추가하지 않는다.
- 다크모드에서만 별도 비주얼 언어를 만들지 않는다. 라이트와 동일한 토큰 구조를 유지한다.

## 구현 체크리스트
- 새 UI 추가 시:
  - 기존 토큰으로 표현 가능한지 먼저 확인
  - 불가능하면 토큰을 추가하고, 최소 2개 이상의 컴포넌트에서 재사용
- 모달 변경 시:
  - 3개 모달(변수 입력/변수 관리/태그 관리)을 같이 확인
  - 드롭다운/탭/버튼/힌트 텍스트가 같은 계층 토큰을 쓰는지 확인
- 상태 확인:
  - 기본, active, selected, disabled, empty-state 모두 점검

## 검증 방법
- 1180x820 기준에서 모달 3종을 모두 열어 색 일관성 확인
- 다크모드 전환 후 회색 계열 고정값이 보이지 않는지 확인
- 선택 상태(탭/칩/버튼)만 단계적으로 더 강한 표면이 적용되는지 확인
